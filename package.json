{
  "name": "rss-merge-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup-kv.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "fmt": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.5.2",
    "wrangler": "^4.10.0"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "linkedom": "^0.18.12"
  }
}
